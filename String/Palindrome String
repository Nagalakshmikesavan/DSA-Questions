class Solution {
    int isPlaindrome(String str) {
        
        String res = "";
       for(int i=str.length()-1;i>=0;i--)
       {
           res += str.charAt(i);
       }
       if(res.equals(str))
            return 1;
        return 0;
       
    }
};

Input: S = "abba"
Output: 1
Explanation: S is a palindrome
